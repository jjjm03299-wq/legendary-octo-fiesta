services:
  - type: web
    name: express-server
    env: node
    plan: free
    buildCommand: npm install express
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
